# Q5 三轴移动手柄验证记录

验证日期：2026-09-10。环境：Qt 6.10.3、MSVC 2022、Vulkan SDK 1.4.357.0。

## 构建与自动测试

最终 `scripts/test.bat` Debug 构建成功，**8/8 测试通过**，无新增 C++ 编译警告。Qt 私有模块的既有版本绑定提示仍保留。

新增 `move_gizmo` 覆盖：

- 三轴分别移动已知的 375 mm，只改变选中轴；按下不跳动，回到起点无累积漂移。
- 正负绝对坐标按 100 mm 吸附，非选中轴保留原值。
- 多组相机距离和场景预览旋转角度下，屏幕命中与毫米位移一致。
- 150% 坐标缩放得到相同位移。
- 中心保留对象拾取区域，背景不命中；近乎正对镜头的轴拒绝不稳定拖动。
- 底心在屏幕外或相机背后时隐藏，零尺寸、奇异矩阵和非有限输入拒绝。
- 三根箭头的绘制范围完整。

原有地面放置、变换编辑、尺寸编辑、场景拾取、场景数据、相机与无显卡启动测试全部通过。

## 双显卡真实窗口回归

最终构建执行 `scripts/test-gpu.ps1 -Rounds 1`：

| 显卡 | 连续帧 | 交换链代数 | 含退出阶段验证错误 | 结果 |
| --- | ---: | ---: | ---: | --- |
| Intel UHD Graphics | 462 | 12 | 0 | PASS |
| NVIDIA GeForce RTX 4060 Laptop GPU | 470 | 12 | 0 | PASS |

新增 `MoveCheck` 在真实 Vulkan 窗口发送鼠标和键盘事件，检查：

- 无选择时没有手柄；选择后三根箭头的杆和箭头区域能读回预期 RGB 像素，绿色轴不会被实体遮住。
- 显示开关改变实际图像，同时控制可交互手柄。
- 长方体与圆柱体分别执行 X/Y/Z 拖动，已知 237 mm 位移写入业务与渲染场景，右侧属性实时同步。
- 选择、尺寸、旋转与其他对象不变；拖动和滚轮不会改变相机。
- Ctrl 无需鼠标移动即可按绝对坐标吸附，释放 Ctrl 恢复自由位置。
- 松开左键保留结果，恢复属性编辑。移动后的实体像素符合独立 CPU 几何参考。
- Esc 恢复快照且保留选择；取消后继续按着左键移动不转动相机，松开不误选对象。
- 右键、失焦、切换对象、隐藏手柄、窗口缩放均恢复起点。
- 对象旋转和场景预览旋转同时存在时，仍沿正确世界轴移动。
- 进入创建模式取消移动并隐藏手柄；替换场景后旧拖动不会污染新场景。

历史选择、属性编辑和创建检查通过实际的“移动手柄”开关关闭覆盖层后执行，保留原有实体像素参考与导航断言；随后打开手柄运行上述新增覆盖层和输入检查。没有放宽旧图像检查的误差范围。原有 Q3/Q4、功能区、创建、缩放及最小化恢复回归继续通过。

已查看手柄初始画面、移动后场景与右侧属性截图，三轴颜色、覆盖显示及数值同步正常。

## 本地证据与范围

- `build/q3-intel-1.txt`、`build/q3-nvidia-1.txt`：逐显卡汇总。
- `build/q3-intel-1.log`、`build/q3-nvidia-1.log`：逐项 `Move ...: 1` 与 `Move overall: 1`。
- `build/q5-move-handles.png`：选中长方体的底心箭头。
- `build/q5-move-result.png`：移动两个对象后的场景。
- `build/q5-move-properties.png`：位置同步后的属性面板。

截图由最后一块显卡覆盖，文本报告逐显卡保留。DPI 检查为数学坐标等价，未修改 Windows 系统显示缩放，自动输入不替代不同鼠标设备上的人工手感检查。本轮不包含松开后的撤销/重做、保存、正交视图、碰撞约束或 1000 对象性能验收。
