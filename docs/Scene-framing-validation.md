# 自动取景验证

日期：2026-09-13。

新版运行包：`out/Nothing3D-0.1.0-win64-20260913-210943.zip`。实际 ZIP 解压后，文件校验、六项功能检查和双显卡启动检查全部通过，测试时清除了开发 Qt 路径。证据目录：`out/package-check-20260913-211001/build`。

- Debug 完整测试：14/14 通过，7.92 秒。
- 新增数学覆盖：1,000 个基本体加旋转对象，0.35/1.7 宽高比，透视/正交，0.001/1/1000 倍场景尺度。逐个检查变换后的 8 个角点处于屏幕与近远裁剪范围内。
- 检查保留观察方向和投影、空场景不改变相机、取景后缩放不跳回旧范围、重置恢复原始相机参数。
- GPU 按钮检查：相机平移到远处后触发“显示全部”，恢复取景且保留选择，成功读回图像。
- GPU 工程检查：打开成功后存在取景参数；显式恢复同一相机后，保存前后像素仍一致。
- Intel UHD：468 帧、13 次交换链创建，整体 PASS，包含退出阶段验证错误 0。
- NVIDIA RTX 4060 Laptop：474 帧、13 次交换链创建，整体 PASS，包含退出阶段验证错误 0。
- 人工检查 `build/fit-scene.png`，箱子和圆柱正常显示。

脚本入口仍为 `scripts/test.bat` 和 `scripts/test-gpu.ps1 -Rounds 1`。相机数学检查覆盖千对象取景，实际窗口工程打开检查使用六对象场景；不把两者混称为千对象窗口操作压力测试。
