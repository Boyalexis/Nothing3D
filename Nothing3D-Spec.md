# Nothing3D Qt/Vulkan 路线规格

## 1. 定位

Nothing3D 是个人学习型三维参数化场景编辑器，不作为深圳图为技术的商业产品，也不承担替代 E3D 的目标。项目采用 C++、Qt 6 和自研 Vulkan 渲染器，重点学习工业三维平台的底层组成。

## 2. 架构边界

```text
Qt Widgets 界面
      ↓
Nothing3D 业务与场景层
      ↓
Nothing3D Vulkan 渲染器
      ↓
Vulkan SDK / 显卡驱动
```

- Qt 负责主窗口、菜单、工具栏、场景树、属性面板和输入事件转发。
- Qt 不负责三维场景数据、基本体生成、相机或渲染资源。
- Nothing3D 自己实现参数化对象、场景组织、三维数学、网格生成、GPU 资源和绘制流程。
- 初期使用 `QVulkanWindow` 完成 Qt 窗口与 Vulkan 表面的可靠集成。
- 项目数据使用自定义 `.n3d` JSON，不依赖 Qt 对象树。
- Python API 延后到编辑器闭环完成之后。

## 3. 第一阶段功能范围

第一阶段功能范围沿用已确认需求：

- 独立 Windows 桌面程序，中文深色界面。
- 左侧扁平场景树、中间三维视图、右侧属性面板。
- 参数化长方体和圆柱体，位置基准为底面中心。
- 输入尺寸后在 `Y=0` 地面点击放置。
- 单选、高亮、三轴移动手柄和属性编辑。
- 默认自由移动；按 `Ctrl` 以 100 mm 吸附。
- 旋转通过属性面板输入；按 `Ctrl` 时以 15°步进。
- 透视、正交、顶视、前视和右视。
- 创建、复制、删除和属性修改支持撤销/重做。
- 手动保存 `.n3d`，支持未保存提示。
- 目标场景为 1,000 个基本体。
- Intel 集显目标约 30 FPS，RTX 4060 目标约 60 FPS。

## 4. 第一阶段明确不做

- 管道、阀门、工程规则、碰撞检查和工业格式导入。
- 多选、框选、分组和层级变换。
- 自动保存、云端、账户和多人协同。
- Python API。
- Qt 3D、Godot、Unity、Unreal 或其他现成三维引擎。
- 光线追踪和高级真实感渲染。

## 5. 技术选择

- C++20
- Qt 6.10.3 Widgets
- CMake 4.4.3
- MSVC 2022 x64
- Vulkan SDK 1.4.357.0（已安装在项目 tools 目录）
- `QVulkanWindow` 初期管理设备、交换链、命令缓冲和帧同步；Nothing3D 自己记录绘制命令。以后按学习需要逐步接管底层资源。
- 第一阶段先手写少量向量、矩阵知识练习，再评估是否引入 GLM
- 后续可引入 Vulkan Memory Allocator，但第一个三角形阶段不使用

## 6. 新路线里程碑

按每周约 6 小时估算。新路线比 Godot 路线明显更长，第一阶段预计需要 8–15 个月；时间是学习估算，不是交付承诺。

### Q0：Qt/C++ 环境和空窗口（约 1–2 周）

- 安装 Qt、CMake，确认 MSVC。
- 创建 CMake C++20 工程。
- 创建 Qt 主窗口、菜单、工具栏、三栏布局和状态栏。
- 建立最小烟雾测试。

完成条件：可从脚本构建、测试并启动 Qt 独立窗口。

### Q1：Qt 中的 Vulkan 清屏（约 2–4 周）

- 安装 Vulkan SDK 与验证层。
- 理解 Vulkan instance、physical device、logical device 和 swapchain。
- 使用 `QVulkanWindow` 创建 Vulkan 窗口。
- 用固定颜色清除窗口并处理窗口缩放。

完成条件：验证层无错误，窗口缩放稳定，状态栏显示实际 GPU。

### Q2：第一个三角形（约 3–6 周）

- 学习顶点、索引、着色器和渲染管线。
- 编译 GLSL 为 SPIR-V。
- 创建顶点缓冲并绘制彩色三角形。

完成条件：能解释一帧从 CPU 到 GPU 的主要步骤。

### Q3：可旋转长方体（约 4–8 周）

- 学习向量、矩阵和坐标空间。
- 生成长方体网格。
- 加入深度缓冲、相机矩阵和基础颜色。

完成条件：可稳定显示并观察旋转的三维长方体。

### Q4：场景和参数化对象（约 6–10 周）

- 建立独立业务对象与渲染对象。
- 支持长方体、圆柱体和多个对象。
- 建立 CPU/GPU 资源生命周期。

### Q5：编辑器交互（约 8–14 周）

- 相机导航、射线拾取、选择高亮。
- 场景树和属性面板同步。
- 基本体创建与三轴移动手柄。

### Q6：命令、保存和测试（约 6–10 周）

- 撤销/重做命令系统。
- `.n3d` 文件、格式版本和未保存提示。
- 自动测试和 1,000 对象性能场景。

## 7. 学习协作方式

- 每次只推进一个可运行的小目标。
- 实现前解释本轮新概念和它在整体架构中的位置。
- 实现后提供运行方式、验收步骤和关键代码入口。
- 复杂 Vulkan 代码按资源创建、每帧绘制和资源销毁三条生命周期解释。
- 当前里程碑以外的新功能先进入候选清单。

## 8. 历史归档

Godot M0 原型和原路线规格保存在 `archive/godot-m0`。它们仅用于回顾，不参与当前构建。
